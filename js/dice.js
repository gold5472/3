function Dice(4 : 8)
this.dicespread = 1;
this.rolled = 0;

}

Dice.prototype.actuate function self.dice {
  if dicechosen = 4;
  var Math.random() < 0.25 ? + : - : × : ÷
  this.rolled = value;
    
  }
  
Dice.prototype.actuate function self.dice {
  if dicechosen = 8;
  var Math.random() < 0.25 ? + : - : × : ÷
  this.rolled = value;
  
}

if (this.rolled = +);
    var
